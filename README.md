# kinet-gpu
KInet GPU core - static library for GPU compute abstraction (linked into libkinetaccel)

# Building

```bash

cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

cmake --build build --config Release

```